# README

## Overview

This example demonstrates how to integrate the **Content Fragment Selector** in a Vanilla JavaScript application.

> **Note:** `@aem-sites/content-fragment-creator` (the Create Content Fragment Dialog) does not ship a UMD/Vanilla JS build and cannot be used from this example. It is currently React only — see the [React example](../react) for a runnable Content Fragment Creator demo.

---
